class Hospital {
  String cidadeId; //Pega a o "id" da cidade dentro do firebase
  String endereco;
  String nome;
  Hospital({
    required this.cidadeId,
    required this.endereco,
    required this.nome,
  });
}
